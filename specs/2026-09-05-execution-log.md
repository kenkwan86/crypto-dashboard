# Execution log for C:/01. Coding/Crypto-Dashboard/specs/2026-09-05-review-plan.md
# adw_id: c62ef8bd
# one line per plan item, written by the factory from the executor's envelope

P1 | Passed | collect_funding stores interval_h parsed from entry['interval'] via _interval_hours, falling back to funding_interval_map (fetch_funding_intervals for binance — documented deviation: ccxt 4.5.28 binance premiumIndex entries carry no interval, verified by probe; bybit '8h' and hyperliquid '1h' entries do), None when nothing is available; backfill_funding adds interval_h from df['funding_interval_hours']; TABLE_KEYS['funding'] unchanged; CLAUDE.md schema row updated. Check: with_iv == n for every exchange (bybit 141/141, hyperliquid 102/102 mn=mx=1.0, binance 150/150 at re-verification; 47/47, 34/34, 50/50 on first run at 17:01 UTC). D1 re-verified this round: null_interval = 0.
P2 | Passed | tools/migrate_funding_interval.py rewrote all 8 funding files in place (temp + os.replace), skip-if-complete guard, per-value row counts printed; hyperliquid 1.0, bybit venue mode 8.0h from one load_markets() call, pre-cutoff binance median-gap snapping with MIN_MONTH_ROWS=10 nearest-month inheritance, post-cutoff nearest-earlier-month. Check: null_interval 0, value set exactly {1.0, 4.0, 8.0}, BTC 8 / ENA 4; idempotent re-run skips all files. Deviation documented in-code: snap set {1,4,8} not {1,2,4,8} because SOL 2022-11 (mixed 2h/8h gaps, median 2h) would yield 2.0 and fail the plan's own subset-of-{1,4,8} assertion; 2h is log-equidistant from 1h and 4h, resolved to 1h.
P3 | Passed | load_funding dedupes max(rate)/max(interval_h) per (symbol,ts,exchange) over a union_by_name=true glob and returns avg(rate * 8.0 / interval_h) WHERE interval_h IS NOT NULL, column still named rate; union_by_name=true added to the other five data_access loaders; Screener caption updated; CLAUDE.md DuckDB example now avg(rate * 8.0 / interval_h)*3*365*100. Check: daily medians from 2026-08-25 onward sit at 0.000100 against late-July 0.000025-0.000100 — no drop to ~1.3e-5; with the P6 fill the panel is flat at 0.000100 across the 08-25 seam (D2 verified by reviewer re-run and this session).
P4 | Passed | oi_zscores returns the single-source continuous frame as 'value' plus a per-symbol 'source' map; load_open_interest() and its tier CASE deleted (no remaining reference by grep); CLAUDE.md false claim replaced with 'Prefer the helpers in analytics/data_access.py.'; Positioning chart title and Screener oi_src column carry the source (0 nulls over 62 symbols). Check: BTC max 1d change 0.108 < 0.35 (0.084 on first run), 09-03→09-04 is 9.178B→8.593B — the 10.23B→15.95B seam is gone, source binance (D3 verified).
P5 | Passed | TABLE_KEYS['liquidations'] = ['symbol','ts','interval']; hourly writes '1h', coinalyze backfill '1d' (both groupbys carry interval — an in-flight bug where the pre-existing groupby dropped the column was caught by the Check showing 2026-08-24 missing, the 55,826 NULL rows were removed, the groupbys fixed, backfill re-run: '+9355 rows'); migration labelled on-disk rows {1d: 67854, 1h: 21929, unknown: 496}, 0 nulls; load_liquidations carries interval; 3_Liquidations and briefing switched to daily_liquidations(). Check: BTC 2026-08-24 = 24.61M long / 35.18M short (1d row; 14 hourly rows < 20), not ~44.5M; reviewer verified 0 duplicate (symbol, day) pairs across 67,870 output rows (D4 met).
P6 | Passed | gap_fill_funding added (cached fetch_funding_intervals lookup, pages fetch_funding_rate_history from the first of the current month, writes interval_h) and called beside backfill_funding in the funding branch; backfill run downloaded the 2026-08 zips for all 50 symbols plus September gap fills. Check: days = 21 exactly for BTC binance funding over [2026-08-04, 2026-08-25), baseline 0 (D5 first half met).
P7 | Passed | Sweep logic unchanged from round 1 and its Check holds (BTC binance OI hours in 48h = 49 at review, 47 on this round's re-query, both >= 46, baseline 14; CLAUDE.md gotcha added). ENVELOPE BLOCKER FIXED: the two independent `deadline = time.monotonic() + SWEEP_BUDGET_S` computations (old lines :126/:178) are replaced by one `sweep_deadline = start + SWEEP_BUDGET_S` created at the top of main (collectors/hourly.py:331, bounded from process start) and passed as a `deadline` parameter into both sweep_funding and sweep_open_interest (signatures at :122/:173, calls at :352-353), so combined sweep runtime is capped at 600s — inside timeout-minutes: 15 with the ~5-min point samples. grep shows SWEEP_BUDGET_S used exactly once as a deadline; py_compile and `import collectors.hourly` both clean; abandon-with-printed-warning behaviour kept.
P8 | Passed | Cached data_freshness() (max ts + age per table from the parquet globs) and render_freshness() (caption + st.error past 3h for funding/OI/liquidations, 30h for options) in dashboard/shared.py; latest() in analytics/zscores.py is now a plain panel.iloc[-1] (second ffill dropped). ENVELOPE BLOCKER (ruled under D6) FIXED THIS ROUND: dashboard/pages/4_Options.py:15 now calls shared.render_freshness() after st.title — all five D6 pages render it (grep: app.py:18, 1_Positioning:15, 3_Liquidations:16, 4_Options:15, 5_Screener:18). Verified live via agent-browser on streamlit :8518 — Options page accessibility snapshot shows StaticText 'Data as of: ohlcv 2026-09-04 17:00:00+00:00 (2.2h old) | funding ... | options_dvol ... | options_chain 2026-09-04 00:00:00+00:00 (19.2h old)' directly under the page heading, above the charts; screenshot logs/d6_options.png; no console errors; bare-mode python dashboard/pages/4_Options.py exits 0. Home and Screener captions verified visually in round 1 (logs/p8_home.png, logs/p8_screener.png).
P9 | Passed | now.hour == 0 gate replaced by options_snapshot_stale() (max options_chain ts earlier than today-floored, or --daily); failure records counts['options'] = 'failed', printed as 'options: FAILED' in the summary. Verified: run at 18:44 UTC (non-00 hour) wrote options_chain +1846 / options_dvol +130 with max chain ts = today 00:00 UTC; an immediate second same-day run added +0 rows to every table; probe options_snapshot_stale() -> False with today's snapshot present.
P10 | Passed | collect_options slices the parsed DVOL candle list [:-1] before building dvol_rows, mirroring collect_ohlcv. Verified after the --daily run: options_dvol max(ts) = 2026-09-04 17:00:00+00:00 < current hour 18:00:00+00:00 (baseline: the max was the 00:00 forming candle).
P11 | Passed | build_context emits units (funding per-8h rate, OI USD with cross_section.oi_src per-symbol source, liquidations USD per symbol-day, mark_iv/atm_iv percent, total_oi 'option contracts (Deribit units), NOT USD') and data_as_of from dashboard.shared.data_freshness; SYSTEM_PROMPT extended with the cite-the-age / refuse-older-than-24h instruction. Check printed both blocks with a max ts + age for all six tables, covering every table feeding cross_section, btc_dvol, options_term_structure and liquidations_daily_14d_usd (reviewer re-confirmed all four consumers present).
